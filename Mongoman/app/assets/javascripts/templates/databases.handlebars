
  <div class="container">
    <div class="list-heading">
      <a href="#/">Databases</a>
      <button class="new-button" {{action 'addDatabase'}}>+ New Database</button>
    </div>

    <div class="invisible" id="newdb-create">Enter the Database name:<br /><br />
      {{input valueBinding="controller.newDbName" size="30" class="dialog-input-box"}}
    </div>

    <div class="invisible" id="db-copy-dialog">Copy as:<br /><br />
      {{input valueBinding="controller.copieddbName" size="30"}}
    </div>

    <div class="invisible" id="db-rename-dialog">Enter new name<br /><br />
      {{input valueBinding="controller.newName" size="30"}}
    </div>

    <div id="placeholder-confirm-drop-db" class="invisible"> Honestly ? </div>

        {{#if isLoaded}}      
          <div class="table-and-widgets-container flex">
              <table class="table-default">
                  <thead>
                    <tr class="table-row">
                          <th>Database Name</th>
                          <th>Collections</th>
                          <th>Size</th>
                          <th>Indexes</th>
                    </tr>
                  </thead>
                  <tbody>
                    {{#each controller}}
                    <tr class="table-row" {{bind-attr class="selected:selected-row"}}>
                      <td>
                        {{view Ember.Checkbox checkedBinding="selected" class="database-selector-checkbox"  classNameBindings="selected:selected-row"}}
                        {{#linkTo 'collections' name class="table-links"}}{{name}}{{/linkTo}}
                      </td>
                      <td>{{collection_count}}</td>
                      <td>{{size}} Gb</td>
                      <td>{{indexes}}</td>
                    </tr>
                    {{/each}}
                  </tbody>
                </table>
                <div>
                  {{partial 'widgets'}}
                </div>
          </div>
        
      {{else}}
          <div style="margin-left: inherit;margin-right: inherit;padding-top: 150px;padding-left: 370px;">
            {{controller.statusText}}
            <div style="margin-left:50px;margin-top:20px;">
                <img src="/spinner.gif" alt="spinner" width="25px;" height="25px;"></img>
            </div>
          </div>
      {{/if}}
  </div>